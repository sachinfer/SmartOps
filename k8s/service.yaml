apiVersion: v1
kind: Service
metadata:
  name: smartops-service
  namespace: smartops
spec:
  type: LoadBalancer
  selector:
    app: smartops-app
  ports:
    - protocol: TCP
      port: 80         # Port exposed to the outside
      targetPort: 80   # Must match the port Uvicorn runs on
